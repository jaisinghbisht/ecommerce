export const environment = {
  production: true,
  // In a real production environment, this would be the URL of your deployed backend.
  // For now, we'll keep it the same, but it should be updated in your CI/CD pipeline.
  apiUrl: 'http://localhost:8080/api/v1'
};
